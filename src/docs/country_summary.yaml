tags:
  - Country Summary
parameters:
  - in: path
    name: country_name
    type: string
    required: true
    description: Country name.

responses:
  200:
    description: A user can get the country summary.

  404:
    description: country summary not found
