tags:
  - Species Summary
parameters:
  - in: path
    name: species_name
    type: string
    required: true
    description: Species name.

responses:
  200:
    description: A user can get the species summary.

  404:
    description: species summary not found
